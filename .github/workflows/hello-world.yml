name: Hello World Workflow
on:
push:
branches:
- main
jobs:
say-hello:
runs-on: ubuntu-latest
steps:
- name: Checkout code
uses: actions/checkout@v2
- name: Print greeting
run: echo "👋 Hello, GitHub Actions!"
